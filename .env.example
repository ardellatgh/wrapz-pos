# Copy to .env.local and fill with your Supabase project values.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only: required for /api/purge (Danger zone). Never expose to the browser.
# After migration 009, purge_event_data is not granted to anon/authenticated.
SUPABASE_SERVICE_ROLE_KEY=
